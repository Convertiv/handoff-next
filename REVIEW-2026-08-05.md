# Staged for review — 2026-08-05

Everything below is **uncommitted, in the working tree**. Nothing was committed or pushed. Delete this file
once you've reviewed it.

`tsc --noEmit` clean · **798 unit + 9 server tests pass** · no new lint warnings (details per file below).

---

## What landed

### Roadmap E.1 — surface what guest authoring built (COMPLETE)

The gap you spotted: the backend existed and nothing linked to it.

| File | What |
|---|---|
| `src/app/components/Share/ShareLinkPanel.tsx` | **new** — mint a **View only** or **Invite to build** link; label, expiry (14d default), max uses; active-links list with usage + revoke |
| `src/app/components/library/AssetInspector.tsx` | takes an optional `shareResource`; when passed, the full panel replaces the old single-link row |
| `src/app/components/Playground/PatternPicker.tsx` | passes `shareResource` — so the panel appears on a saved page's inspector |
| `src/app/app/api/handoff/share/links/route.ts` | **new** — `GET` active links for a resource, gated on `canChangeVisibility` |
| `src/app/lib/db/grant-queries.ts` | `listShareLinks` (+ `ShareLinkSummary`), `countReviewQueue` |
| `src/app/app/library/page.tsx` · `LibraryClient.tsx` | **Review queue** entry with a pending badge, maintainer-only, counted server-side |

**The thing to look at first:** a write-capable link's secret is hashed, so it can be shown **exactly
once**. The panel says so in the amber note, and an existing link renders as "The full URL can't be shown
again" rather than displaying an id that looks like a URL and 404s for whoever receives it. That behaviour
is deliberate — please sanity-check the wording.

Verified against the DB (read-only apart from rows it deleted after): both link shapes list correctly,
`submissionCount` counts pages built through a link, and neither summary leaks a secret.

### Roadmap E.3 — pages as documents (MOSTLY DONE — see "not done")

| File | What |
|---|---|
| `src/app/app/playground/[id]/page.tsx` | **new** — `/playground/{id}` is a real route; unknown id → 404 rather than a silent empty canvas |
| `src/app/lib/pattern-payload.ts` | **new** — `buildPatternPayload` extracted from `SavePatternDialog` so autosave and the dialog write an identical record |
| `src/app/components/Playground/PlaygroundContext.tsx` | debounced (2s) **autosave** when the page has a record; `saveState`; local-storage auto-restore **removed** |
| `src/app/components/Playground/PlaygroundBuilder.tsx` | save-state indicator in the toolbar; recovery banner |

**The clean-canvas fix, and why it isn't a deletion.** The old code restored
`localStorage['handoff-playground-components']` on mount, which is why "New" reopened old work. Deleting
the key outright would have thrown away anyone's unsaved canvas on the deploy that shipped it — so it is
now read **into an offer**: a banner saying "You have an unsaved canvas from a previous visit (N blocks)"
with *Restore it* / *Start fresh*. Either choice clears the key, so it appears at most once, and a page
opened by id never shows it (the record is the truth there).

**Autosave scope, deliberately narrow.** It writes `components` + `data` only — never title/description/
group/tags, because those are edited elsewhere and sending empty strings would wipe them. It also refuses
to fire on the first observation after a load, or the load itself would immediately save what it just read.
A failed save shows "Not saved" rather than retrying silently.

---

### Roadmap E.2 — one save path, and templates as separate objects (BUILT, needs your eyes)

Built to your reframing: **"save this page as a template"** creates a *separate, frozen, team-visible
copy*. Your page carries on as yours. That also fixes something I'd flagged — a frozen template means a
guest submission's diff stays stable instead of shifting under whoever reviews it next.

| File | What |
|---|---|
| `src/app/lib/db/pattern-write.ts` | `savePageAsTemplate` (new record: `source: 'template'`, `visibility: 'team'`, snapshot of blocks + guardrails) and the **freeze guard** in `patchPattern` |
| `src/app/app/actions/patterns.ts` | `savePatternAsTemplate` server action |
| `src/app/components/Playground/PlaygroundContext.tsx` | **save on first block** — creates the record, redirects to `/playground/{id}` |
| `src/app/components/Playground/PlaygroundBuilder.tsx` | save button → **Save as template**; static mode keeps an explicit save; result notice |
| `src/app/app/api/handoff/patterns/[id]/clone/route.ts` | a clone of a template now records `template_id`, so editor pages are diffable like guest ones |
| `src/app/app/library/LibraryClient.tsx` etc. | wording: "New page", "Pages" |

**The freeze is enforced in the write core, not the UI** — because autosave reaches `patchPattern`, so
opening a template and nudging a block would otherwise rewrite the team's standard silently. Verified
against the DB, eight behaviours:

1. template created with `source: template`, `visibility: team`, blocks **and guardrails** copied
2. source page untouched (still `playground`, still `private`, same title)
3. editing the page still works
4. editing template **content** → refused
5. an autosave-shaped write (`data` only) to a template → refused
6. an administrative meta change (archive) → still allowed, correctly
7. templating a template → refused with a useful message
8. provenance recorded: `{action: 'save-as-template', fromPageId}` — *not* in `template_id`, which means
   "built from" and would have inverted every diff that reads it

Decisions I made rather than asking, flag if you disagree: a template is created `status: 'draft'` (setting
`approved` here would route around the maintainer-gated approve path); anyone who can edit a page can
template it (the dangerous act — sending a link outside the team — is separately gated); and the
browser-local template *creation* path is gone while existing local templates stay loadable, so they can be
converted rather than lost. **Full removal of that code path is the one piece of E.2 still outstanding.**

## Not done, and why

**In-iframe accessibility checking** — moved to `docs/BACKLOG.md` per your call, with the reason it isn't
small (it changes the preview contract, not just a lib).

**Guardrail editing UI for templates** — Slice 3 reads limits from `template.data.guardrails` and enforces
them, but nothing sets them from the browser yet; today it is a JSON field. That is the natural next piece
now that templates are real objects.

**Removing the local-template code path** (`saveAsTemplate`/`loadTemplate`/`deleteTemplate`,
`TemplateManager`'s local section) — creation is disabled, the rest stays until someone confirms nobody has
local templates worth keeping.

---

## What I could not verify

Everything authenticated. `/review`, the share panel, the playground autosave and the recovery banner all
need a signed-in maintainer session, and I won't type credentials. What *is* verified: typecheck, the full
test suite, the new SQL exercised against the real DB, and every new endpoint returning 401/403 to
unauthenticated callers.

**Worth 10 minutes of your own clicking, in this order:**

1. `/library` → is the **Review queue** entry there, and does the badge match reality?
2. Open a saved page's inspector → **Links** → create an *Invite to build* link → does the copy-once
   warning read right? Open the link in a private window: name gate → edit → submit.
3. `/review` → does the submission appear with the diff and the guardrail annotations? Approve it.
4. `/playground` with old local state → does the recovery banner appear instead of auto-loading?
5. `/playground/{id}` on a saved page → edit a block, wait 2s → does the toolbar say **Saved**, and does a
   reload show the change?
6. `/playground` → drop one block → does the URL become `/playground/{id}` on its own, with "Saved"?
7. On a saved page → **Save as template** → does it appear in the library as team-visible, and is your page
   still yours and still editable? Then open the template itself and try to edit a block: it should refuse
   rather than silently rewrite the standard.

Step 4 is the one I'd most like eyes on, because it is the only change here that alters behaviour you
already rely on.
