<div class="hero-dev-5 d-flex align-items-center" style="background-image: url('/api/component/hero-dev-5-asset-0.png');">
  <div class="container d-flex justify-content-between align-items-center">
    <div class="text-content">
      <nav class="breadcrumb text-muted mb-2">
        <span>PREVIOUS</span> / <span class="fw-bold">CURRENT</span>
      </nav>
      <h1 class="display-4 fw-bold">{{headline}}</h1>
      <p class="lead text-muted">{{subheadline}}</p>
      <div class="d-flex gap-3 mt-4">
        <a href="#" class="btn btn-primary">{{primaryCTA}}</a>
        <a href="#" class="btn btn-outline-primary">{{secondaryCTA}}</a>
      </div>
    </div>
    <div class="image-content">
      <img src="/api/component/hero-dev-5-asset-1.png" alt="Decorative charts">
    </div>
  </div>
</div>