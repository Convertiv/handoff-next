<div data-component="hero-dev-6" class="hero-dev-6 d-flex align-items-center p-4 bg-light rounded-3" style="background-image: url({{background_image}}); background-size: cover;">
  <div class="content col-6">
    <div class="text-muted mb-2">{{subheading}}</div>
    <h1 class="fw-bold mb-3">{{main_heading}}</h1>
    <div class="mb-4">{{{paragraph}}}</div>
    <div class="d-flex gap-3">
      <a href="#" class="btn btn-primary">{{primary_cta}}</a>
      <a href="#" class="btn btn-secondary">{{secondary_cta}}</a>
    </div>
  </div>
</div>