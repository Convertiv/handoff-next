# Stack guide: react-scss

- **Templates:** React TSX with colocated `.scss` modules or BEM-style classes
- **CSS:** SCSS + design tokens as CSS variables
- **Avoid:** Bootstrap unless the project explicitly mixes stacks

Use the project `handoff.config.js` entries for component roots.
