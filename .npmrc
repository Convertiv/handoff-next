# .npmrc
engine-strict=true

# If this package lives under the Handoff npm workspace (../package.json), the parent
# should use install-strategy=nested (see ../.npmrc). postinstall also copies drizzle-kit
# locally when hoisting would break drizzle-kit + drizzle-orm pairing.
